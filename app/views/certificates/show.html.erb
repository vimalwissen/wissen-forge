<!DOCTYPE html>
<html>
<head>
  <title>Certificate - <%= @badge.name %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    @media print {
      .no-print { display: none; }
    }
  </style>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
  <div class="bg-white border-8 border-double border-gray-800 p-12 max-w-4xl w-full text-center shadow-2xl relative">
    <div class="absolute top-4 right-4 no-print">
      <button onclick="window.print()" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">Print</button>
      <%= link_to "Back", profile_path, class: "bg-gray-500 text-white px-4 py-2 rounded hover:bg-gray-600 ml-2" %>
    </div>
    
    <div class="border-4 border-gray-200 p-8 h-full">
      <h1 class="text-4xl font-serif text-gray-800 font-bold mb-8 uppercase tracking-widest">Certificate of Achievement</h1>
      
      <p class="text-xl text-gray-600 mb-4">This is to certify that</p>
      
      <h2 class="text-3xl font-cursive text-indigo-700 font-bold mb-6 underline"><%= current_user.name %></h2>
      
      <p class="text-xl text-gray-600 mb-8">has successfully completed the requirements for the</p>
      
      <h3 class="text-2xl font-bold text-gray-800 mb-2"><%= @badge.name %> Badge</h3>
      <p class="text-gray-500 italic mb-12"><%= @badge.level.humanize %> Level</p>
      
      <div class="flex justify-between items-end mt-16 px-16">
        <div class="text-center">
          <div class="border-b border-gray-400 w-48 mb-2"></div>
          <p class="text-sm text-gray-500">Date Awarded</p>
          <p class="font-medium"><%= @user_badge.awarded_at.to_date.to_fs(:long) %></p>
        </div>
        
        <div class="text-center">
          <!-- Placeholder Signature -->
          <div class="font-cursive text-xl mb-2 text-gray-800">WissenForge L&D</div>
          <div class="border-b border-gray-400 w-48 mb-2"></div>
          <p class="text-sm text-gray-500">Authorized Signature</p>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
